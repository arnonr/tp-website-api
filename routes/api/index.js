// import express from "express";
// const router = express.Router();
// import News from "./news.js";

// router.use(`/api/v${process.env.API_VERSION}`, router.use("/news", News));

// router.use("/news", News);

// export default router;
